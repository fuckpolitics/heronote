# URL бэкенда. Если не задан — фронт берёт текущий хост и порт 3000
# (работает и на localhost, и по локальной сети).
# Для прода укажи адрес API, например:
# VITE_API_URL="https://api.example.com"
VITE_API_URL=
